# deploy/compose

The local, no-cloud `docker compose` stack: both lock backends, the rail and the services, runnable
without a Google Cloud account (NFR-15). Owned by milestone **M0 — Foundations** through T-005 (local
compose stack); this placeholder from T-001 is replaced by T-005's README.
